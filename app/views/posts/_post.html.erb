<div class="card">
    <div class="card-header">
        <h1><%= post.member.name %></h1>
        <small><%= post.member.username%></small>
    </div>
    <div class="card-body">
        <%= post.body %>
    </div>
</div>